{
  "private": true,
  "scripts": {
    "start": "yarn workspace @undataforum/components start",
    "purge": "rm -rf ./node_modules && yarn workspaces run purge",
    "lint": "eslint . --ignore-path .gitignore",
    "format": "pretty-quick"
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "git add"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged && lint-staged"
    }
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.0.3",
    "eslint": "6.5.1",
    "eslint-plugin-shopify": "30.0.1",
    "husky": "3.0.7",
    "lint-staged": "9.4.1",
    "now": "16.3.1",
    "prettier": "1.18.2",
    "pretty-quick": "1.11.1"
  }
}
