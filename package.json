{
  "private": true,
  "scripts": {
    "build": "yarn workspace @undataforum/components run build",
    "purge": "rm -rf ./node_modules && yarn workspaces run purge",
    "lint": "eslint . --ignore-path .gitignore",
    "format": "pretty-quick"
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "git add"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged && lint-staged"
    }
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@changesets/changelog-github": "0.2.0",
    "@changesets/cli": "2.4.1",
    "eslint": "6.8.0",
    "eslint-plugin-shopify": "33.0.0",
    "husky": "3.1.0",
    "lint-staged": "9.5.0",
    "now": "16.7.0",
    "prettier": "1.19.1",
    "pretty-quick": "2.0.1"
  }
}
