import Helmet from "react-helmet";

<Helmet
  title="@undataforum/components - color"
  meta={[{ name: "description", content: "Type color." }]}
/>

# Type color

Color is of type `oneOf` and accepts the following string values:

- `primary`
- `blue`
- `green`
- `red`
- `yellow`
- `gray`
- `black`
- `white`
