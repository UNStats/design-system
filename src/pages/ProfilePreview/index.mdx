import Helmet from "react-helmet";
import CompleteProfileExample from "./complete-profile-example.mdx";
import AlignLeftExample from "./align-left-example.mdx";
import AlignRightExample from "./align-right-example.mdx";
import AvatarOnlyExample from "./avatar-only-example.mdx";
import AvatarAndNameOnlyExample from "./avatar-and-name-only-example.mdx";
import ResponsiveAvatarExample from "./responsive-avatar-example.mdx";
import CustomFontSizeExample from "./custom-font-size-example.mdx";
import ReallyLongStringsExample from "./really-long-strings-example.mdx";

<Helmet
  title="@undataforum/components - ProfilePreview"
  meta={[{ name: "description", content: "A preview for a post." }]}
/>

# ProfilePreview

Preview for a profile.

## Props

`ProfilePreview` includes all props from type [profile](/types/profile/) except `id` and `href`. It also includes the following props:

| Prop       | Description                   | Type                                     | Required | Default  |
| :--------- | :---------------------------- | :--------------------------------------- | :------- | :------- |
| `align`    | `ProfilePreview` alignment.   | `oneOf(["left", "center", "right"])`     | `false`  | `center` |
| `fontSize` | Font size for profile `name`. | `oneOfType([numeric, arrayOf(numeric)])` | `false`  | `[3, 4]` |

In addition `ProfilePreview` accepts [margin and padding props](https://rebassjs.org/props/#margin-and-padding).

## Examples

### Complete profile

In this example we render a complete profile. If `profile` does not containe `href`, the `ProfilePreview` is not linked.

<CompleteProfileExample />

[Fullscreen](/ProfilePreview/complete-profile-example/)

```jsx
<ProfilePreview
  profile={{
    avatar: () => (
      <Image
        alt="Nance MacFadin"
        borderRadius="circle"
        height={128}
        width={128}
        src="/avatar0.png"
      />
    ),
    name: "Nance MacFadin",
    affiliation: {
      jobtitle: "Help Desk Operator",
      organization: "Schuster-Senger"
    },
    badges: [
      { text: "Committee Member", color: "red", href: "/committee" },
      { text: "Keynote Speaker", color: "yellow", href: "/keynote-speakers" }
    ]
  }}
  m={2}
/>
```

### Align left

In this example we align the `ProfilePreview` left.

<AlignLeftExample />

[Fullscreen](/ProfilePreview/align-left-example/)

```jsx
<ProfilePreview
  profile={{
    avatar: () => (
      <Image
        alt="Clerc Edis"
        borderRadius="circle"
        height={128}
        width={128}
        src="/avatar1.png"
      />
    ),
    name: "Clerc Edis",
    affiliation: {
      jobtitle: "Staff Scientist",
      organization: "Ritchie Inc"
    },
    badges: [
      { text: "Committee Member", color: "red", href: "/committee" },
      { text: "Keynote Speaker", color: "yellow", href: "/keynotes" }
    ]
  }}
  align="left"
  m={2}
/>
```

### Align right

In this example we align the `ProfilePreview` right.

<AlignRightExample />

[Fullscreen](/ProfilePreview/align-right-example/)

```jsx
<ProfilePreview
  profile={{
    avatar: () => (
      <Image
        alt="Clifford Skipperbottom"
        borderRadius="circle"
        height={128}
        width={128}
        src="/avatar2.png"
      />
    ),
    name: "Clifford Skipperbottom",
    affiliation: {
      jobtitle: "Senior Financial Analyst",
      organization: "Block, Bruen and Gerlach"
    },
    badges: [
      { text: "Committee Member", color: "red", href: "/committee" },
      { text: "Keynote Speaker", color: "yellow", href: "/keynotes" }
    ]
  }}
  align="right"
  m={2}
/>
```

### Avatar only

In this example we render the `ProfilePreview` as avatar only.

<AvatarOnlyExample />

[Fullscreen](/ProfilePreview/avatar-only-example/)

```jsx
<ProfilePreview
  profile={{
    avatar: () => (
      <Image
        alt="Willow Rangall"
        borderRadius="circle"
        height={64}
        width={64}
        src="/avatar3.png"
      />
    )
  }}
  m={2}
/>
```

### Avatar and name only

In this example we render the `ProfilePreview` as avatar with name.

<AvatarAndNameOnlyExample />

[Fullscreen](/ProfilePreview/avatar-and-name-only-example/)

```jsx
<ProfilePreview
  profile={{
    avatar: () => (
      <Image
        alt="Suzy Cahan"
        borderRadius="circle"
        height={128}
        width={128}
        src="/avatar4.png"
      />
    ),
    name: "Suzy Cahan"
  }}
  m={2}
/>
```

### Responsive avatar

In this example we make the avatar's image responsive.

<ResponsiveAvatarExample />

[Fullscreen](/ProfilePreview/responsive-avatar-example/)

```jsx
<ProfilePreview
  profile={{
    avatar: () => (
      <Image
        alt="Sandra Abramovici"
        borderRadius="circle"
        height={[32, 64, 128]}
        width={[32, 64, 128]}
        src="/avatar5.png"
      />
    ),
    name: "Sandra Abramovici"
  }}
  m={2}
/>
```

### Custom font size

In this example we set a custom font size for the name.

<CustomFontSizeExample />

[Fullscreen](/ProfilePreview/custom-font-size-example/)

```jsx
<ProfilePreview
  profile={{
    avatar: () => (
      <Image
        alt="Jan McKea"
        borderRadius="circle"
        height={128}
        width={128}
        src="/avatar6.png"
      />
    ),
    name: "Jan McKea",
    fontSize: [5, 6]
  }}
  m={2}
/>
```

### Really long strings

In this example we render a profile with really long strings.

<ReallyLongStringsExample />

[Fullscreen](/ProfilePreview/really-long-strings-example/)

```jsx
<ProfilePreview
  profile={{
    avatar: () => (
      <Image
        alt="Really really really really really really really really really really long name"
        borderRadius="circle"
        height={128}
        width={128}
        src="/avatar7.png"
      />
    ),
    name:
      "Really really really really really really really really really really long name",
    affiliation: {
      jobtitle:
        "Really really really really really really really really really really long jobtitle",
      organization:
        "Really really really really really really really really really really long organization"
    },
    badges: [
      { text: "Committee Member", color: "red", href: "/committee" },
      { text: "Keynote Speaker", color: "yellow", href: "/keynote-speakers" }
    ]
  }}
  m={2}
/>
```
